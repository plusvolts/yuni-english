# 윤이 영어 — 설치 방법

이 폴더 전체가 앱이에요. 인터넷 주소(https)에 올린 뒤 탭 A8이나 폰의 **크롬**에서 설치해요.
음성인식과 오프라인 실행은 https 주소에서만 작동해요.

## 1. 올리기 (둘 중 하나)

### A. GitHub Pages (추천: 무료, 수정이 쉬움)
1. GitHub에서 새 저장소를 만들어요 (예: `yuni-english`, Public).
2. 이 폴더의 파일을 모두 올려요 (`index.html`이 저장소 맨 위에 있어야 해요).
3. 저장소 Settings → Pages → Branch: `main` / `(root)` → Save.
4. 1~2분 뒤 `https://<아이디>.github.io/yuni-english/` 주소가 생겨요.

### B. Netlify Drop (가장 빠름: 가입 후 끌어다 놓기)
1. https://app.netlify.com/drop 에 접속해요.
2. 이 폴더를 통째로 끌어다 놓으면 주소가 바로 생겨요.

## 2. 탭 A8·폰에 설치
1. **크롬**으로 위 주소를 열어요. 삼성 인터넷은 음성인식이 안 될 수 있어요.
2. 오른쪽 위 ⋮ 메뉴 → **앱 설치** (또는 "홈 화면에 추가")를 눌러요.
   홈 화면에 **윤이 영어** 아이콘이 생기고, 일반 앱처럼 전체 화면으로 실행돼요.
3. 처음 마이크 버튼을 누르면 뜨는 **마이크 권한 허용**을 눌러요.
4. 한 번 실행한 뒤에는 인터넷이 없어도 돼요. 단, 음성인식은 인터넷이 필요해요.

## 3. (선택) APK 파일로 만들기
1. https://www.pwabuilder.com 에 앱 주소를 넣어요.
2. Package for stores → **Android** → Download를 누르면 APK가 만들어져요.
3. 태블릿에서 APK를 열어 설치해요 ("출처를 알 수 없는 앱" 허용 필요).

## 4. 내용 고치기
- 단어·질문: `content.js` (파일 맨 위 설명 참고). 주제마다 단어 50개(하루 5개씩 10일), 질문 20개(하루 2개씩 10일)예요.
  목록 순서대로 1일차, 2일차…에 새로 나오니, 쉬운 단어를 앞쪽에 두세요.
- 고친 뒤 `sw.js`의 `VERSION` 숫자를 올려요 (예: `yuni-english-1` → `yuni-english-2`).
  설치된 앱을 두 번 껐다 켜면 반영돼요.
- 가족·친구 사진: `photos/` 폴더에 넣고 content.js 단어에 `photo: 'photos/hyun.jpg'`를 추가해요.

## 5. 아빠 화면
홈 오른쪽 위 ⚙️ → 곱셈 문제를 풀면 들어가요.
주간 요약, 자주 틀리는 단어, 진도 코드 옮기기, 백업, 시간 제한, 별 목표를 설정할 수 있어요.

## 6. 업데이트해도 진도·설정이 그대로 남게 하려면
진도·별·보상·설정은 **탭의 크롬 안에, 앱 주소별로** 저장돼요. 그래서 아래 세 가지만 지키면 새 버전을 올려도 그대로 남아요.
1. **주소를 바꾸지 않기** — 같은 GitHub 저장소(같은 주소)에 파일만 덮어써서 올려요. 저장소 이름을 바꾸거나 새 저장소를 만들면 다른 앱으로 인식돼서 기록이 안 보여요.
2. **앱을 지우지 말고 그대로 두기** — 새 버전을 올린 뒤 설치된 앱을 두 번 껐다 켜면 자동으로 바뀌어요. 다시 설치할 필요가 없어요.
3. **크롬의 "인터넷 사용 기록 삭제 → 쿠키 및 사이트 데이터"를 하지 않기** — 이걸 지우면 기록도 지워져요.

앱을 꼭 지웠다 다시 설치해야 한다면, 먼저 아빠 화면 → **💾 백업 파일 저장**을 눌러 백업 파일을 받아두세요.
다시 설치한 뒤 아빠 화면 → **📂 백업 파일 불러오기**로 그 파일을 고르면 모두 돌아와요.

## 7. 한국어 목소리가 잘 안 들릴 때
한국어 안내와 설명은 태블릿에 들어 있는 음성(TTS)으로 읽어요. 기기 설정에 따라 목소리 품질이 크게 달라요.
1. 태블릿 **설정 → 일반 → 글자 읽어주기(TTS)** 로 가요. (메뉴 이름은 "텍스트 음성 변환"일 수도 있어요)
2. **기본 엔진**을 **Google 음성 인식 및 합성**으로 바꿔요. (없으면 Play 스토어에서 "Google 음성 인식 및 합성" 설치)
3. 엔진 옆 ⚙️ → **음성 데이터 설치** → **한국어**를 받아요.
4. 앱을 껐다 켠 뒤 아빠 화면 → **한국어 목소리**에서 목소리를 골라 **🔈 한국어 들어보기**로 비교해요. **한국어 말 속도**도 바꿀 수 있어요.
